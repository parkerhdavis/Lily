# Cargo configuration for Lily backend.
# Redirects Cargo output to the repo-root target/ (portfolio convention: all
# build artifacts live under /target, not per-app). Path is relative to this
# file, so apps/desktop/backend/.cargo/config.toml + "../../../target" lands
# at the repo root.

[build]
target-dir = "../../../target"
